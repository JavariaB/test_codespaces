{
  "name": "my-php-app",
  "build": {
    "dockerfile": "."
  },
  "remoteUser": "codespace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/<workspace-name>,type=bind,consistency=cached",
  "extensions": [
    "ms-vscode.vscode-typescript-tslint-plugin"
  ]
}
